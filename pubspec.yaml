name: glass_app_toast 
description: "A fully customizable Flutter toast notification with glass effect, swipe to dismiss, and platform-aware icons."
version: 1.0.0
homepage: https://github.com/CHIVA001/package-flutter
repository: https://github.com/CHIVA001/package-flutter/tree/app_toast
environment:
  sdk: ^3.12.0
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
